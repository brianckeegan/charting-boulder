# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#pdm.lock
#   pdm stores project-wide configurations in .pdm.toml, but it is recommended to not include it
#   in version control.
#   https://pdm.fming.dev/latest/usage/project/#working-with-version-control
.pdm.toml
.pdm-python
.pdm-build/

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# pytype static type analyzer
.pytype/

# Ruff stuff:
.ruff_cache/

# PyPI configuration file
.pypirc

# Other
.DS_Store
/.claude

# Node build deps + local env (widget build / pipeline)
node_modules/
.env.local
.env.*.local

# Reader-response data — NEVER commit to a public repo. The survey export and the
# notebook's analysis frame contain individual (if anonymous) reader submissions,
# including self-reported demographics. The notebook writes the analysis frame on
# every run, so without these rules a routine `git add -A` would publish it.
responses.csv
2026-06-budget-tool/pipeline/responses.csv
2026-06-budget-tool/budget-survey-analysis-frame.csv

# Newspack Iframe Block archive (generated by package-newspack-iframe.sh)
2026-06-budget-tool/boulder-budget-interactive.zip

# Omit original BLS source data (re-downloadable; regenerated into processed CSVs).
# Non-anchored so the rule survives the raw dirs being moved between subfolders.
# oews_*_raw/ covers the metro/state/national (and legacy boulder) zip folders.
oews_*_raw/
oews_raw/
qcew_*_raw/
qcew_boulder_raw/

census_api.json
