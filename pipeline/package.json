{
  "name": "boulder-budget-pipeline",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Serverless pipeline that writes Balance Boulder's Budget contributions to Supabase and serves the anonymous aggregate.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  }
}
